{
  "name": "Streamlit Conda Environment",
  "image": "mcr.microsoft.com/devcontainers/python:conda-3.12",
  "postCreateCommand": "conda env create -f environment.yml && conda init bash && echo '✅ Conda environment created'",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  },
  "forwardPorts": [8501],
  "portsAttributes": {
    "8501": {
      "label": "Streamlit App",
      "onAutoForward": "openPreview"
    }
  },
  "postAttachCommand": "bash -c 'source ~/.bashrc && conda activate base && streamlit run pericarditis_app.py --server.enableCORS false --server.enableXsrfProtection false'"
}
